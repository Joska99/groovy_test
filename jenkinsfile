node('agent1') {
    stage('test') {
        sh 'ls -lah'
    }
    stage('Multy branch') {
        if (env.BRANCH_NAME == 'main') {
            echo 'main trigger'
        } else {
            echo 'other'
        }
    }
}

