node('agent1') {
    stage('test-1') {
        sh 'ls -lah'
        sh "echo ${env.BRANCH_NAME}"
    }
    stage('Multy-branch') {
        if (env.BRANCH_NAME == 'main') {
            echo 'main trigger'
        } else {
            echo 'other branch'
        }
    }
}

