node('agent1') {
    stage('test-1') {
        sh 'ls -lah'
        sh 'env'
    }
    stage('Multy-branch') {
        
        def branchName = env.GIT_BRANCH ?: 'Unknown'

        if (branchName == 'main') {
            echo 'main trigger-11'
            echo $branch
        } else {
            echo 'other branch'
        }
    }
}

