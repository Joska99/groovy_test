node('agent1') {
    stage('test') {
        sh 'ls -lah'
        sh 'env'
    }
    stage('Multy-branch') {
        def branchName = env.GIT_BRANCH ?: 'Unknown'
        if (branchName == 'origin/main') {
            echo 'main trigger-11'
            echo "$branchName"
        } else {
            echo 'other branch'
            echo "$branchName"
        }
    }
}

